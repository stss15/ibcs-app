# A3.3.2 · Construct queries between two tables in SQL

## Learning intentions
- Write joins combining two tables with relational operators, filtering, pattern matching, ordering.
- Use SQL clauses SELECT, DISTINCT, FROM, WHERE, BETWEEN, ORDER BY, GROUP BY, HAVING, JOIN, LIKE, AND/OR/NOT.
- Interpret query plans for efficiency considerations.
