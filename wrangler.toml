name = "ibcs-auth"
main = "src/worker.js"
compatibility_date = "2024-05-01"

# Bindings are configured via Wrangler commands during deployment.
# wrangler secret put TOKEN_SECRET
# wrangler secret put INSTANT_APP_ID
